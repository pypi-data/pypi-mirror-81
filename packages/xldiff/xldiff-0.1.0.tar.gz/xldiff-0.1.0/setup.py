#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['xldiff']

package_data = \
{'': ['*']}

install_requires = \
['openpyxl']

entry_points = \
{'console_scripts': ['xldiff = xldiff:main']}

setup(name='xldiff',
      version='0.1.0',
      description='A dead-simple command line diffing of datasets stored in Excel',
      author='George Lesica',
      author_email='george@lesica.com',
      url='https://github.com/glesica/xldiff',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.6',
     )

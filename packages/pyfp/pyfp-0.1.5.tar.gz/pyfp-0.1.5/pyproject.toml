[tool.poetry]
name = "pyfp"
version = "0.1.5"
description = "A library for functional programming patterns."
authors = ["Brett Kolodny <brettkolodny@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0a5"]
build-backend = "poetry.core.masonry.api"

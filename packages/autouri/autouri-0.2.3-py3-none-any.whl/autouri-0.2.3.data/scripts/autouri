#!python
try:
    from autouri.cli import main
except Exception:
    import os
    import sys

    script_path = os.path.dirname(os.path.realpath(__file__))
    sys.path.append(os.path.join(script_path, "../"))
    from autouri.cli import main

if __name__ == "__main__":
    main()

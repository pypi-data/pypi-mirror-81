version: 1
disable_existing_loggers: false
formatters:
  standard:
    format: '[ %(levelname)s ] %(name)s : %(message)s'
handlers:
  console:
    class: logging.StreamHandler
    level: DEBUG
    formatter: standard
root:
  level: INFO
  handlers:
  - console

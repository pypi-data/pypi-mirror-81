from setuptools import setup

setup(name='distributions_pablo_baby',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_pablo_baby'],
      zip_safe=False)

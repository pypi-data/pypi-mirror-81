#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['distiller', 'distiller.json', 'distiller.markup']

package_data = \
{'': ['*']}

install_requires = \
['pydantic >=1.0.0']

extras_require = \
{'dev': ['devtools'],
 'html': ['beautifulsoup4 >=4.8.1', 'lxml'],
 'test': ['pytest >=4.0.0',
          'pytest-cov',
          'mypy',
          'black',
          'isort > 5',
          'faker',
          'flake8']}

setup(name='pydantic-distiller',
      version='0.1.0b7',
      description='HTML/JSON documents parser into Pydantic models',
      author='Arseny Gabdullin',
      author_email='a.gabdullin@tinkoff.ru',
      url='https://github.com/tinkoffjournal/distiller',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      python_requires='>=3.6',
     )
